# Main Agent Memory

Deprecated. The CLI no longer loads or updates MEMORY.md files.

Durable coordination lessons have been merged into `CLAUDE.md`.

Current note: repeated `Centering`/`Placing` should use object-specific safe drop poses from `world_summary.objects_by_id` when available; runtime also applies small final XY variation.
